% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dematel_spectral.R
\name{dematel_spectral_analysis}
\alias{dematel_spectral_analysis}
\title{DEMATEL Spectral Analysis}
\usage{
dematel_spectral_analysis(
  D,
  T,
  case_name = NULL,
  return_eigenvalues = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{D}{Normalized direct influence matrix (n x n numeric matrix)}

\item{T}{Total relations matrix (n x n numeric matrix)}

\item{case_name}{Optional name/identifier for this analysis}

\item{return_eigenvalues}{Logical, whether to return all eigenvalues (default: FALSE)}

\item{verbose}{Logical, whether to print detailed output (default: TRUE)}
}
\value{
List containing spectral analysis results
}
\description{
Performs spectral analysis on DEMATEL total relations matrix to extract
enhanced insights into system dynamics and stability characteristics.
}
\examples{
# Create example matrices
D <- matrix(runif(25, 0, 0.15), 5, 5)
diag(D) <- 0
D <- D / max(rowSums(D), colSums(D))  # Normalize
T <- D \%*\% solve(diag(5) - D)

# Perform analysis
results <- dematel_spectral_analysis(D, T, case_name = "Example")

# Access specific results
print(results$lambda_max)
print(results$is_diagonalizable)
}
